# Analysis Manifest

> **2026-09-02: all quantitative entries below were re-run after five defects were
> fixed** (media blanks inside the analysis matrix, unused artifact columns,
> per-feature log2FC pseudocount, unpaired blank rule, mixed null distributions).
> Analysis matrix is now **60 fungal samples x 26,574 features** (was 90 x 38,547;
> an interim 25,157 reflected an over-aggressive adduct filter, corrected 2026-09-03).
> Read `CORRECTED_REANALYSIS_REPORT.md` first — several previously reported
> numbers and two sub-claims are retracted there.


<!-- Add entries below using the appropriate manifest entry template. -->

## sirius-annotation

- **Status**: transfer + native **full run merged** — 4,957 local features annotated (1,773 transferred + 3,184 native `native-full-e9838293-bagel`; 4,268 with structure; 0 new merge-conflicts from the native fold-in; the pre-existing 69 merged-conflict formula-disagreeing features remain flagged). Transfer: 2,182/2,860 EB SIRIUS-annotated features matched (76.3%). Native pipeline: pilot job `27605104` (149 spectra, 5 shards, completed 2026-08-20, superseded) validated shard size 30; full run job `27718540` (3,927 charge-1+ targets, 3,810 usable spectra, 127 shards) completed 2026-08-25 (one shard, `_11`, TIMEOUT'd on the array's 1h limit and was resubmitted standalone with `--time=02:00:00`, job `27748769_11`), merged via `merged_full/` and folded in 2026-08-25.
- **Purpose**: Attach SIRIUS 6.3.12 formula/structure/CANOPUS annotations to the GNPS2 Everything-Bagel features by m/z+RT+MS2-cosine transfer from the sibling EB project; native SIRIUS run fills the remaining un-annotated charge-1+ features (3,927 targets, ~30 spectra/shard for failure/restart handling) and folds in via `--native-merged`.
- **Key inputs**: `../EB/analysis/sirius_annotation/{sirius_annotations.tsv,sirius_targets.csv,sirius_targets.mgf}`, `data/raw/gnps2_e9838293_bagel/nf_output/feature_finding/aligned_features_filled.mgf`, `.../feature_finding_results/aligned_features.csv`.
- **Key outputs**: `analysis/sirius_annotation/{sirius_annotations.tsv,sirius_transfer_map.tsv,sirius_native_targets.csv,sirius_native_targets.mgf,shards_native/,sirius_native_results/,scripts/{import_sirius_transfer.py,select_native_targets.py,export_native_mgf.py,run_sirius_native.sh,run_sirius_native.sbatch}}`.
- **Readme**: [SIRIUS_ANNOTATION.md](sirius_annotation/SIRIUS_ANNOTATION.md)

## ordination

- **Status**: complete (bagel feature table) — all-samples + per-species Bray-Curtis PCoA outputs present (`figures/pcoa_all.{pdf,png}`, `figures/pcoa_condition.{pdf,png}`, `figures/pcoa_stagegroup.{pdf,png}`, `figures/pcoa_axes_all.csv`, `figures/by_species/pcoa_{dendrobatidis,salamandrivorans}.{png,pdf}`); replicates EB F-001 (matrix dominance). **Re-run 2026-09-03 on 60 fungal samples x 26,574 features**: axis1 **58.5%** all / **69.9%** Bd / **65.7%** Bsal (previously 62.9/75.9/70.6 — inflated by the 30 media blanks). Matrix separates COMPLETELY on PCoA1 (liq [+0.207,+0.320], spore [-0.370,-0.126], no overlap), and PCoA2 now resolves a monotonic spore stage gradient (Zoo +0.246 / Spor -0.074 / Mat -0.172) against a flat liq fraction. Pipeline is fully reproducible via `pixi run pcoa-ordination`. New figures color by the sampled condition (6-state `matrix_life_stage`) and by the collapsed life-stage `stage_group` (Zoospore vs Developed).
- **Purpose**: Bray-Curtis PCoA of the feature table across species x matrix (liq/spore) x life_stage (Zoospore/Sporangium/Mature) (GOALS.md goal 3), the Everything-Bagel port of the EB/Rhodotorula pattern.
- **Key inputs**: `data/raw/gnps2_e9838293_bagel/nf_output/feature_finding/feature_finding_results/aligned_features.csv`, `data/metdata/curated_gnps_metadata.tsv` → `analysis/ordination/linked_data/` (built by `scripts/build_ordination_table.py`).
- **Key outputs**: `analysis/ordination/{linked_data/{sample_metadata.csv,feature_abundance.csv.gz},figures/{pcoa_axes_all.csv,pcoa_all.png,pcoa_all.pdf,pcoa_condition.png,pcoa_stagegroup.png,by_species/*}}`.
- **Readme**: [ORDINATION.md](ordination/ORDINATION.md)

## differential_features

- **Status**: complete (bagel feature table) — all 30 pairwise contrasts (15 per species), **re-run 2026-09-03** on 60 fungal samples x 26,574 features with an exact permutation null. Read the counts alongside `separation_enrichment.tsv`: 5 of the 12 within-matrix stage pairs report 0 significant yet are 3.4x-17.0x enriched over the null — `n_significant` is a step function of the feature universe at n=5v5 (see the separation-enrichment entry). SIRIUS identity join not applied at this tier (EB pattern)
- **Purpose**: Feature-level pairwise differential abundance between every (matrix, life_stage) state pair, within each species (GOALS.md goal 2), the Everything-Bagel port of the sibling projects' scripts.
- **Key inputs**: `analysis/ordination/linked_data/` tables (built from the bagel `aligned_features.csv` + curated metadata), `analysis/sirius_annotation/sirius_annotations.tsv` (not yet joined).
- **Key outputs**: `analysis/differential_features/{comparison_summary.csv,<species>_<condA>_vs_<condB>/{differential_features.csv.gz,volcano.png/.pdf,top_features.png/.pdf,top_features.tsv}}`. Repro: `pixi run differential-features`.

## differential_features_primary

- **Status**: complete; **`is_secreted_candidate` media-blank-corrected 2026-09-02** — 8 primary contrasts with volcano/top_features + SIRIUS-annotated significant-features tables. 4 collapsed life-stage contrasts (Zoospore vs Developed, within species x matrix: n=5 vs 10 per matrix per species after the media-blank removal) + 4 secreted-vs-cellular contrasts (liq vs spore, within species x stage_group). SIRIUS annotation join applied to the merged annotation table (see sirius-annotation, now the full native run); significant feature-rows flagged for liq-vs-spore enrichment and curated bioactivity keywords → **61,613 significant rows, 1,750 bioactivity-flagged** (2026-09-03 corrected re-run). As of 2026-09-02 `is_secreted_candidate` = `is_liq_enriched AND passes_media_blank` (>=2x its `C_liq` companion blank in >=1 life stage, via `background_subtraction.fungal_over_blank_ratio`); the old raw definition is retained as `is_liq_enriched`. With the paired-by-plate blank rule this cuts 38,806 liq-enriched rows to **3,071** secreted candidates (7.9%) — both media are peptide-rich broths, so medium peptides were scoring as maximally 'secreted'. Corrected per-contrast counts: Bd liq Zoo-vs-Dev 505, Bd spore 3,733, Bsal liq 2,384, Bsal spore 4,381, liq-vs-spore 9,327-16,263.
- **Purpose**: Hypothesis tier on the collapsed life-stage vocabulary (F-002: matrix stratification is required; Sporangium+Mature collapse is power-motivated). Answers "zoospore vs the rest" (life stage) and "secreted fraction vs cell-associated" (matrix) questions behind GOALS.md goals 2-4.
- **Key inputs**: `analysis/ordination/linked_data/` tables, `analysis/sirius_annotation/sirius_annotations.tsv`.
- **Key outputs**: `analysis/differential_features_primary/{primary_comparison_summary.tsv,significant_annotated.tsv,significant_bioactive.tsv,feature_tables_index.html,all_significant_features_summary.tsv,all_significant_features_summary_<species>.html,...}` — note the interactive rollup HTML is chunked per species (dendrobatidis/salamandrivorans) to stay under GitHub's 100 MB per-file limit. Repro: `pixi run differential-features-primary` then `pixi run feature-tables-primary` (sortable/filterable feature-table HTML per contrast + per-species rollups, Rhodotorula strategy).
- **Readme**: [DIFFERENTIAL_FEATURES_PRIMARY.md](differential_features_primary/DIFFERENTIAL_FEATURES_PRIMARY.md)

## genome_bioactivity_linkage

- **Status**: complete end-to-end run for both species (2026-08-25), using this project's own local fallback for every BFD functional-annotation input (BFD's own shared `pfam_hmmscan`/`signalp`/`predgpi`/`antismash_local` run still has zero output for either `Batrachochytrium` locustag). `link_compounds_to_genes.build_candidate_table`'s `is_extracellular` requirement was relaxed from a hard filter to an informational column (`require_extracellular=False` default, 2026-08-25) after the strict gate produced only 0 (dendrobatidis) / 2 (salamandrivorans) candidate rows — zero overlap between domain-hit proteins and the extracellular-scored set, expected since NRPS/PKS/P450/terpene-synthase enzymes are cytoplasmic. Relaxed-gate output: `dendrobatidis` 2,634 rows (147 compounds × 32 proteins; tier 1/2/3 = 144/2,487/3), `salamandrivorans` 8,322 rows (277 compounds × 66 proteins; tier 1/2/3 = 275/4,432/3,615). `is_extracellular=True` on only 0/2 of those rows respectively — the relaxation, not some other bottleneck, is what unlocked candidate recovery. Pass `require_extracellular=True` to restore the original strict secreted-pathway-only behavior.
- **Purpose**: Link SIRIUS/CANOPUS compound-class annotations of background-filtered, liq-enriched differential features to candidate biosynthetic genes (PFAM domain family + BGC-region membership + secretion prediction + NCBI-ortholog cross-reference) per species, tiered 1 (best) to 3 (weakest evidence).
- **Key inputs**: BFD gene predictions/`.gbk` (`/bigdata/stajichlab/shared/projects/BFD/Fungi_BFD_runs/genome_annotation/<out>/`); local fallback runs under `analysis/genome_bioactivity_linkage/results/{pfam_hmmscan,signalp,predgpi,deeptmhmm,rbh,antismash_ncbi,merops}/<species>/` (`run_pfam.sh`, `run_signalp.sh`, `run_predgpi.sh`, `run_deeptmhmm.sh`, `run_rbh.sh`, `run_antismash_reference.sh`, `run_merops.sh`); `analysis/sirius_annotation/sirius_annotations.tsv`; `analysis/differential_features_primary/all_significant_features_summary.tsv`; `data/metdata/curated_gnps_metadata.tsv`.
- **Key outputs**: `analysis/genome_bioactivity_linkage/results/{dendrobatidis,salamandrivorans}_candidate_table.tsv`. Repro: `pixi run gbl-build-tables` (after `gbl-fetch-reference` + the `run_*.sh`/`.sbatch` local-fallback jobs).
- **Follow-ups resolved 2026-08-26**: candidate tables now carry RNA-seq expression columns (`rna_is_expressed` etc., via `analysis/rnaseq_expression/`); tier-1 NRPS BGC characterization at `genome_bioactivity_linkage/TIER1_NRPS_CHARACTERIZATION.md`; Bsal protein-count/duplication diagnostics at `results/duplication_check/`; `parse_pfam_domains.py` domtblout offsets spot-checked against real files (correct).
- **New (2026-08-26)**: secreted-protease candidate tables (MEROPS, not compound-linked) — `analysis/genome_bioactivity_linkage/results/{dendrobatidis,salamandrivorans}_protease_candidates.tsv` via `pixi run gbl-protease-candidates`, dominated by MEROPS family M36 (fungalysin/deuterolysin, a known secreted fungal virulence-factor family). See `genome_bioactivity_linkage/PROTEASE_CANDIDATES.md`.

## rnaseq_expression

- **Status**: complete — 8 public RNA-seq runs (Bd JEL423 3×, Bsal AMFP13 5×) aligned to the NCBI reference via STAR, then **per-gene counts recomputed with featureCounts** from gffread-converted GTFs. STAR's own `--quantMode GeneCounts` produced no gene counts (NCBI GFF3 exon lines carry no `gene_id` attribute) — fixed with `gffread -T` (GTF) + `featureCounts` (unstranded `-s 0`, the best-assigning mode) over the existing BAMs. `build_expression_evidence.py` maps each linkage candidate BFD protein through its RBH NCBI ortholog to a locus and folds `rna_is_expressed` / `gene_total_raw` / `n_rep_ge_min` / `rna_no_evidence` into `{dendrobatidis,salamandrivorans}_candidate_table.tsv`.
- **Purpose**: 4th independent (measured) boolean in the linkage tiering — presence/absence expression evidence for candidate biosynthetic genes (NOT condition-matched co-expression; these runs are not from the LC-MS growth condition).
- **Key inputs**: `data/raw/rnaseq_srp291769/<SRR>/` fastq; `results/star_index/<species>/`; `results/star_align/<SRR>/…bam`; `results/gtf/<species>.gtf` (gffread); NCBI `genomic.gff`; `genome_bioactivity_linkage/results/rbh/` pairs.
- **Key outputs**: `results/gene_counts/<species>/counts_s{0,1,2}.txt` + `gene_expression.tsv`; expression columns on the candidate tables. Repro: `pixi run rna-expression-evidence` (after the indexed `run_featurecounts.sh` SLURM job).
- **Readme**: [RNASEQ_EXPRESSION.md](rnaseq_expression/RNASEQ_EXPRESSION.md)

## differential_features_primary (ordinal life-stage trend)

- **Status**: complete (re-run 2026-09-03) — Spearman rho of TSS-normalized abundance vs stage rank (Zoospore=0/Sporangium=1/Mature=2, the real 8/48/96 h axis), BH-FDR per species × matrix stratum. Added because the `Zoospore | Developed` collapse was justified on a false premise (learnings L-21). Both species have a 3-state spore trajectory — the "Bsal is 2-state" reading was itself retracted, since a 0 at n=5v5 is a BH-threshold artifact, not absence of signal (Bsal spore Spor-vs-Mature is 12.4x separation-enriched). Stage pairs are NOT re-run (the 30-way scan covers them); this tests monotonic progression, which the collapse destroys. Results (per-feature permutation null, 2026-09-03): Bd liq 3,139 of 17,629 (595 blank-clearing); Bd spore 4,542 of 12,651; Bsal liq 4,747 of 20,191 (1,102); Bsal spore 3,943 of 13,284 — consistently more signal than the collapsed contrasts. An earlier pooled null inflated these ~9-11%.
- **Purpose**: recover the developmental-trajectory signal that collapsing Sporangium+Mature discards, per species × matrix.
- **Key inputs**: `analysis/ordination/linked_data/`, `analysis/sirius_annotation/sirius_annotations.tsv`, `background_subtraction.py` (media-blank column).
- **Key outputs**: `analysis/differential_features_primary/lifestage_trend/{<species>_<matrix>_trend.tsv,trend_summary.tsv,trend_<species>_<matrix>.png/.pdf}`. Repro: `pixi run lifestage-trend`.
- **Readme**: [DIFFERENTIAL_FEATURES_PRIMARY.md](differential_features_primary/DIFFERENTIAL_FEATURES_PRIMARY.md)

## differential_features_primary (liq-enriched USI curation)

- **Status**: complete; **media-blank-corrected 2026-09-02** — per-species ranked lists of liquid-vs-spore-enriched significant features joined to SIRIUS identity and GNPS library hits, each with live metabolomics-USI link-outs (render/mirror/json). Validated: the resolver returns real peak arrays for the top hits. Bd: 13,170 liq-enriched features (1,187 = 9.0% clear the `C_liq` media blank; 1,659 SIRIUS-structure, 82 lib-hit); Bsal: 7,179 (1,436 = 20.0% clear the blank; 656 structure, 44 lib-hit). Rows are now ordered `passes_media_blank` first, so the top-100 MS² grid went from 2/100 (Bd) and 7/100 (Bsal) blank-clearing to 100/100; non-clearing rows are amber-tinted, none are dropped. **The four MS² priority targets previously named in `handoff.txt` (Bd 473/884, Bsal 2953/975) are all at or below their media blank** — both media are peptide-rich broths, so medium peptides score as maximally liq-enriched without being secreted. Current blank-clearing top hits: Ahpatinin Pr, Mullinamide A (Bd); Mycosubtilin D, Isopedopeptin E, Miporamicin (Bsal).
- **Purpose**: manual MS² verification scaffold — the step that turns sequence-based candidates into compound-level curation; several top hits are NRPS-peptide-type (e.g. Bd `H-Leu-Leu-Phe-Gly-Nle-Pro-Val-`, Bsal `cyclo[Phe-D-Phe-Val-DL-Pro-Pro...]`) worth comparing against the tier-1 NRPS BGCs.
- **Key inputs**: `differential_features_primary/all_significant_features_summary.tsv`, `data/raw/gnps2_e9838293_bagel/nf_output/feature_library_search/merged_feature_library_search_results.tsv`.
- **Key outputs**: `differential_features_primary/liq_enriched_curation/{dendrobatidis,salamandrivorans}_liq_enriched_top.tsv` + `_liq_enriched_usi.html`. Repro: `pixi run usi-curation`.
- **Readme**: [GENOME_BIOACTIVITY_LINKAGE.md](genome_bioactivity_linkage/GENOME_BIOACTIVITY_LINKAGE.md)

## differential_features (separation enrichment)

- **Status**: complete (re-run 2026-09-03). Threshold-free replacement for `n_significant` as the stage-pair summary statistic. At n=5v5 the minimum attainable two-sided Mann-Whitney p is `2/C(10,5)=7.94e-3`, so BH can call nothing unless `k >= p_min*m/q` ~ 16% of features separate perfectly at once — making `n_significant` a step function of the feature universe. Reports complete-separation counts vs the analytic null plus the per-contrast BH-callability threshold. Result: **all 12 within-matrix stage pairs are enriched 3.4x-44.7x over the null (exact label-permutation p<=0.024); 7 of 12 are BH-callable and 5 are not**; `BH_sig` is non-zero exactly when the separation count clears `k_needed`. This refutes the "Sporangium and Mature are indistinguishable" premise behind the stage collapse.
- **Key outputs**: `analysis/differential_features/separation_enrichment.{tsv,png,pdf}`. Repro: `pixi run separation-enrichment`.

## differential_features (exact permutation null)

- **Status**: complete (2026-09-02). `scripts/mwu_exact.py` enumerates the full conditional Mann-Whitney null (252 assignments at 5v5, 3,003 at 5v10; 20,000 sampled at 10v10), used by both differential tiers. Replaces scipy `mannwhitneyu`, whose `method="auto"` mixes exact and asymptotic nulls (letting tie-heavy features beat perfectly separated ones) and whose `method="asymptotic"` is 1.5x conservative at 5v5 (floor 1.219e-2 vs exact 7.937e-3 — enough alone to drive one contrast from 5,507 significant to 0). Validated: max |perm p - scipy exact p| = **0.0** on 500 tie-free 5v5 features and 1.1e-16 at 5v10; floors reproduce 7.937e-3 / 6.660e-4 exactly; handles the ties scipy's exact method refuses.

## differential_features_primary (peptide origin test — RETRACTED)

- **Status**: **RETRACTED 2026-09-02** (kept for the audit trail; do not cite its conclusion). The test compared Pro composition parsed from SIRIUS names against external substrate references and is invalidated by three controls: the whole SIRIUS table parsed identically is **19.5% Pro+Hyp**, so the shortlist values (Bd 24.3% p=0.23, Bsal 16.9% p=0.42) are indistinguishable from the database baseline; against each species' own non-blank-clearing medium peptides (19.7% / 18.2%) the shortlist differs at Fisher p=0.27 / 0.75; and the reference substrate was wrong (tryptone is whole casein, ~11.3% Pro, which Bd's 24.3% rejects). A compositional statistic over database-assigned identities inherits the database's composition. **Replaced by** the hydroxyproline-immonium contrast (86.0600 in 26.3% of Bsal liq peptide MS2 spectra vs 9.2% Bd; OR 3.53, p=7.1e-16), which is fragment-level and has a built-in negative control since only Bsal's medium contains gelatin. Original description follows. Superseded status: Tests whether the blank-clearing, MS2-backed liq peptides are medium-protein digest products (H2) or NRPS products (H1), by parsing residue composition from SIRIUS structure names (rejecting 43/62 Bd and 72/103 Bsal names as non-peptide rather than guessing) and binomial-testing Pro(+Hyp) frequency against beta-casein (~17%), collagen/gelatin (~22%) and an average proteome (~5%). Result: **Bd 24.3% (27/111 residues), Bsal 16.9% (34/201)** — both consistent with casein/gelatin, both rejecting the proteome baseline at **p=5.8e-12 / 5.3e-10**. Supports H2, converging with the MEROPS M36 fungalysin expansion in Bsal. Caveat: composition is inferred from names, not MS2 fragment ladders, and SIRIUS's DB is peptide-biased — this tests the class, not any single feature.
- **Key outputs**: `analysis/differential_features_primary/peptide_origin/{peptide_origin.tsv,peptide_composition.png/.pdf}`. Repro: `pixi run peptide-origin`.

## molecular_network

- **Status**: complete (2026-09-02) — first attempt at GOALS.md goals 1 and 4. Component traversal validated against GNPS2's own `ComponentIndex` (0 spanning edges, 1:1 partition). Raw network 9,600 edges / 4,423 nodes / 659 components (largest 97) shrinks to **551 components, largest 47** once artifact rows are removed — families were inflated by isotope/adduct/ISF copies of one molecule cosine-matching itself. **Negative result for the secretion hypothesis**: against background blank-clearing rates of 4.16% (Bd) / 7.28% (Bsal), only 5 / 8 components are blank-enriched (Fisher BH q<0.05, >=3 members) and only **2** (both Bsal) are entirely blank-clearing. Species split independently reproduces the peptide finding: Bd's blank-enriched families are Fatty acids (glycerophospholipids — plausibly cell-derived), Bsal's are proline-rich Amino acids and Peptides. DeltaMZ ladders real but minor: 237/2,781 intra-matrix edges (8.5%) classify (CH2 68, 2xCH2 49, H2 49, O 44).
- **Purpose**: molecular-family-level evidence, which is stronger than per-feature evidence at n=5 with a medium background that mimics the hypothesis.
- **Key inputs**: `data/raw/gnps2_e9838293_bagel/nf_output/networking/filtered_pairs.tsv`, `analysis/ordination/linked_data/`, `analysis/sirius_annotation/sirius_annotations.tsv`, `background_subtraction.py`.
- **Key outputs**: `analysis/molecular_network/{components.tsv,delta_mz_ladders.tsv,component_summary.md,component_sizes.png/.pdf}`. Repro: `pixi run network-components`.
- **Readme**: [MOLECULAR_NETWORK.md](molecular_network/MOLECULAR_NETWORK.md)

## differential_features_primary (media consumption)

- **Status**: complete (2026-09-02). Complement of the enrichment analysis: features **depleted** relative to their own plate blank are medium components the fungus consumed or transformed — the most robust liquid-fraction readout here, since it does not require the feature to be fungal in origin. Depletion grows monotonically with culture age in both species (Bd 58 -> 300 -> 1,911; Bsal 103 -> 1,107 -> 3,568 across Zoospore/Sporangium/Mature). **Both species preferentially deplete peptide-class features** (Bd 91.9% vs 76.2% background, OR 3.87, p=1.8e-11; Bsal 93.9%, OR 5.64, p=1.6e-22), and **only Bsal also releases a peptide-enriched set** (OR 2.07, p=1.9e-05) while Bd's released material is peptide-poor (OR 0.60, n.s.) — the asymmetry an expanded secreted M36 protease repertoire predicts. Media differ between species so absolute counts are not cross-comparable; the compositional Fisher tests are within-species against each species' own background, so the medium confound cancels.
- **Key outputs**: `analysis/differential_features_primary/media_consumption/{consumption.tsv,class_enrichment.tsv,consumption.png/.pdf}`. Repro: `pixi run media-consumption`.

## peptide_provenance

- **Status**: complete (2026-09-03). De novo MS2 sequence-tag matching of the 267 blank-clearing/MS2-backed liq shortlist spectra against the six growth-medium substrates (bovine alphaS1/alphaS2/beta/kappa casein + collagen alpha1(I)/alpha2(I), 3,677 aa, cached in `reference_material/substrate_proteins/`). **Result NEGATIVE**: aggregate enrichment **1.56x** over a composition-matched decoy, Wilcoxon **p=0.15**, 4/46 spectra at p<0.05 vs 2.3 expected (binomial p=0.20). The test has validated power — a synthetic b/y spectrum of beta-casein 60-68 scores **55.2x** and a random peptide **0.0x**. Three design choices were forced by controls and are each the opposite of the obvious one: match tags in BOTH orientations (b-series reads N->C, y-series C->N); randomize the TAG not the substrate (a reversed-sequence decoy scores exactly 1.00x against a known casein peptide, and shuffling collagen inflates the decoy vocabulary 20% because Gly-X-Y repeats give only 0.43 distinct 3-mers per residue); and use 5-residue tags, since 3-mers cover 21.4% of k-mer space and show 0.98-1.00x at every peak depth. Directional hint: all 4 nominally-significant spectra are Bsal, none Bd, and only Bsal's medium contains gelatin — same direction as the independent Hyp-immonium result, but not significant alone.
- **Purpose**: settle H1 (NRPS product) vs H2 (medium-protein digest) for the liq shortlist using spectra rather than database names, after the proline-composition test was retracted for inheriting SIRIUS's database composition.
- **Key inputs**: `aligned_features_filled.mgf`, `liq_enriched_curation/<species>_liq_enriched_top.tsv`, `reference_material/substrate_proteins/*.fasta` (UniProt P02662/P02663/P02666/P02668/P02453/P02465).
- **Key outputs**: `analysis/peptide_provenance/{fragment_tag_hits.tsv,sensitivity_sweep.tsv,fragment_tag_sweep.png/.pdf}`. Repro: `pixi run fetch-substrates && pixi run fragment-tags`.
- **Readme**: [PEPTIDE_PROVENANCE.md](peptide_provenance/PEPTIDE_PROVENANCE.md)
